{{#each posts as |post|}}
<div class="card">
    <div class="post-title">
        <h2><a href="/post/{{post.id}}">{{post.title}}</a></h2>
        <p>{{post.date_created}}</p>
    </div>
    <div>
        <p>{{post.text}}</p>
    </div>
</div>
{{/each}}